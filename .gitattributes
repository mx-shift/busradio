# Normalize text to LF in the repo; default working-tree EOL is LF.
* text=auto eol=lf

# DOS toolchain sources and song files check out as CRLF.
src/*       text eol=crlf
songs/*.TXT text eol=crlf

# Binary assets — never touch.
songs/*.WAV binary
